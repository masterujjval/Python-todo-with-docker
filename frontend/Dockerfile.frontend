FROM node:18-alpine
WORKDIR /app
COPY package*.json /app/
RUN npm install
COPY . .
RUN npm run build

# Stage 1
FROM nginx:alpine
COPY --from=0 /app/dist/ /usr/share/nginx/html/
CMD ["nginx","-g","daemon off;"]

